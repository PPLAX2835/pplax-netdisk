<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xyz.pplax.pplaxnetdisk.mapper.UserFileMapper">

    <select id="selectPageVo" parameterType="xyz.pplax.pplaxnetdisk.domain.UserFile" resultType="xyz.pplax.pplaxnetdisk.vo.file.FileListVO">
        select * from user_file a
        left join image on a.fileId = image.fileId
        left join file on file.fileId = a.fileId
        <where>
            <if test="fileTypeId != null">
                <choose>
                    <when test="fileTypeId != 5">
                        extendName in (select fileExtendName from file_classification where fileTypeId = #{fileTypeId})
                    </when>
                    <otherwise>
                        extendName not in (select fileExtendName from file_classification where fileTypeId in (1, 2, 3, 4))
                    </otherwise>
                </choose>
                and a.isDir = 0
            </if>
            <if test="userFile.userId != null">
                and a.userId = #{userFile.userId}
            </if>
            <if test="userFile.filePath != null">
                and a.filePath = #{userFile.filePath}
            </if>
            <if test="userFile.extendName != null">
                and a.extendName = #{userFile.extendName}
            </if>
            <if test="userFile.userFileId != null">
                and a.userFileId = #{userFile.userFileId}
            </if>
            <if test="userFile.fileName != null">
                and a.fileName = #{userFile.fileName}
            </if>
            and a.deleteFlag = 0
        </where>
        ORDER BY isDir desc
    </select>

    <select id="selectUserFileByLikeRightFilePath" resultType="xyz.pplax.pplaxnetdisk.domain.UserFile">
        select * from user_file
        where (filePath = #{filePath} or filePath like concat(#{filePath},'/%')) and userId = #{userId} and deleteFlag = 0
    </select>

    <select id="selectStorageSizeByUserId" resultType="java.lang.Long" parameterType="java.lang.String">
        SELECT SUM(fileSize) FROM user_file
        LEFT JOIN file ON file.fileId = user_file.fileId
        WHERE user_file.userId = #{userId}
    </select>

</mapper>