<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xyz.pplax.pplaxnetdisk.mapper.RecoveryFileMapper">

    <select id="selectRecoveryFileList" resultType="xyz.pplax.pplaxnetdisk.vo.file.RecoveryFileListVo">
        SELECT * FROM recovery_file a
        LEFT JOIN user_file b ON a.userFileId = b.userFileId
        left join file c on c.fileId = b.fileId
        WHERE userId = #{userId}
    </select>
</mapper>